## Review — priority-s — Phase D2

### PASS
- Tests: `pnpm test` passed at current HEAD in `packages/editor` — **35 files, 402 tests, 402 passing, 0 failing**.
- Type check: `pnpm typecheck` clean in `packages/editor`.
- Lint: `pnpm lint` clean in `packages/editor`.
- Modularity under corrected standards: reviewed Priority S production files stay within the documented **300-line file** threshold and remain cohesive by responsibility (`packages/editor/src/tools/terminal-read/stubs.ts` 269 lines, `runtime-buffer.ts` 152, `terminal-run.ts` 184, `extension.ts` 149).
- No new `console.log`, `TODO`, or `FIXME` markers found in the reviewed Priority S production scope.

### FAIL — must fix before Phase E
- **correctness / boundary-proof / docs-alignment** — `packages/editor/src/tools/terminal-read/stubs.ts:211-252`, `packages/editor/src/tools/terminal-read/runtime-buffer.ts:63-77`, `packages/editor/src/extension.ts:134-139`, `packages/editor/src/__tests__/terminal-observe-preview.test.ts:108-120`, `packages/editor/src/__tests__/terminal-observe-authority.test.ts:29-48` — the runtime still has no live terminal-output capture path. `createTerminalReadDeps()` installs a stub `TerminalOutputSource`, the only real runtime objects wired in `activate()` are `buffer.read` and `redactor.redact`, and the buffer’s `append()` path is unreachable from production code. The passing S-TR-09/S-TR-11 tests still rely on mocked `buffer.read` output or static schema assertions, not a real registered tool call with captured terminal output. **Why release is blocked:** S-TR-04, S-TR-09, and S-TR-11 are not closed on the required runtime/public boundary, so `accordo_terminal_run(observeMaxLines > 0)` and follow-up `accordo_terminal_read` cannot yet be trusted to return real shared-buffer output. **Done when:** wire a real VS Code terminal-output source into the shared buffer append path, invoke tracking on the real runtime path, and add package-integration or runtime proof that a real command produces non-empty observed output, redacts matching secrets, and remains continuable through `accordo_terminal_read`. **Required proof surface:** package integration or MCP/runtime proof per `docs/test-plan-terminal-readback.md:101-104` and `docs/30-development/coding-guidelines.md:183-185`. **Source tag:** `TASK-SPECIFIC` (`S-TR-04`, `S-TR-09`, `S-TR-11`; `docs/20-requirements/requirements-editor.md:1475,1478,1528-1535`, `docs/20-requirements/requirements-editor.md:528-531`) + `DOC-STANDARD` (`docs/30-development/coding-guidelines.md:183-185`).
- **correctness** — `packages/editor/src/tools/terminal-read/runtime-buffer.ts:41-45,99-107` — the runtime cursor encoder emits `t-<terminalId>:<payload>`, but the incremental-read parser expects `request.since.split(":")` to have at least three segments and therefore never decodes the returned cursor format. In the live buffer implementation, follow-up reads will restart at line 0 instead of continuing after the prior cursor. **Why release is blocked:** S-TR-02 and the continuity half of S-TR-11 are still broken in the real runtime implementation even before MCP wiring is considered. **Done when:** make cursor parsing consume the actual emitted format and add a test against the real runtime buffer/read path proving that a second read using the first response cursor excludes previously returned content. **Required proof surface:** helper or package-integration proof using the real `runtime-buffer.ts` implementation. **Source tag:** `TASK-SPECIFIC` (`S-TR-02`, `S-TR-11`; `docs/20-requirements/requirements-editor.md:1473,1478,1533`) + `DOC-STANDARD` (`docs/30-development/coding-guidelines.md:134-136`).
- **boundary-proof** — `packages/editor/src/tools/terminal/terminal-lifecycle.ts:6-21`, `packages/editor/src/__tests__/terminal-read-lifecycle.test.ts:99-129` — the production close path now calls `terminalOutputBuffer.clearTerminal`, but the approval-required proof is still missing: the lifecycle test manually invokes `deps.buffer.clearTerminal(...)` and explicitly documents that it is only checking seam existence, not the registered close event or the real runtime buffer. **Why release is blocked:** S-TR-05 is not closed on the required boundary, so buffer reset and stale-cursor invalidation after a real terminal close remain unproven. **Done when:** replace the seam-only proof with package-integration or runtime proof that the registered terminal-close lifecycle fires, clears retained output, and causes a stale cursor to be rejected through the real `accordo_terminal_read` path. **Required proof surface:** package integration or MCP/runtime proof per `docs/test-plan-terminal-readback.md:101-104,126` and `docs/30-development/coding-guidelines.md:183-185`. **Source tag:** `TASK-SPECIFIC` (`S-TR-05`; `docs/20-requirements/requirements-editor.md:1476,1535`) + `DOC-STANDARD` (`docs/30-development/coding-guidelines.md:183-185`).

### S-TR closure status at current HEAD
- Closed on reviewed implementation/tests: **S-TR-01, S-TR-03, S-TR-06, S-TR-07, S-TR-08, S-TR-10**.
- Not closed: **S-TR-02, S-TR-04, S-TR-05, S-TR-09, S-TR-11**.

### Blocker-set completeness
- This is the full known blocker set for this patch based on current HEAD review evidence.
