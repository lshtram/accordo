/**
 * accordo-marp — scaffold smoke test
 * Verifies the test infrastructure works.
 */
import { describe, it, expect } from "vitest";

describe("accordo-marp scaffold", () => {
  it("test runner works", () => {
    expect(1 + 1).toBe(2);
  });
});
